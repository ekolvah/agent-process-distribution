## 0. Delivery start

- [x] 0.1 Gate: `grep -q "^approve" openspec/changes/v2-2f-start-change/architect-review.md` exits 0 (on `rework`: apply the findings, re-review, no delivery task runs) and `gh issue view 144 --json projectItems --jq '.projectItems[0].status.name'` prints `Planned` (tracking issue 144); otherwise print `propose run not finished: run its tail (the Architect review entry of this rule) first` and stop — nothing is asked and nothing is created here
- [x] 0.2 Branch: `git fetch origin && git checkout main && git pull` → `gh issue develop -c 144 --name v2-2f-start-change`; verify `git branch --show-current` prints the change name
- [x] 0.3 `python .agent-process/scripts/set_status.py 144 "In Progress"` exits 0 (the priority is already set on the issue)
- [x] 0.4 Provenance: `gh issue comment 144 --body "planner: Claude; implementer: <this carrier>"` (Claude or Codex)

## 1. RED first

- [x] 1.1 `tests/publisher/test_delivery_scripts.py`: add `{"id": "S_PLAN", "name": "Planned"}` to the Status options of `_FIELDS`; extend `_Gh` with `gh issue view … --json projectItems` (a `status` the test sets), `gh issue develop`, `gh issue comment`, `gh issue create`. Add one test per exit path of `start_change`: `test_verdict_is_rework` (tmp change dir whose `architect-review.md` says `rework` → `start_change.main([...])` exits 2, `_Gh.calls` has no `gh issue develop`), `test_propose_run_stopped_before_its_tail` (tasks.md whose Group 0 token still carries the placeholder → 2 with `propose run not finished`; tasks.md reading `tracking issue 7` in Group 0 and the literal placeholder in a later line → not refused on that ground; tasks.md reading `tracking issue 7` and `_Gh` answering `gh issue view … --json projectItems` with Status `Todo` → 2 with the same line and `Todo`; `_Gh` answering `{"projectItems": []}` → 2 with the same line and `Status: none`), `test_tasks_of_a_new_change_start` (Status `Planned` → `gh issue develop -c 7 --name <change>`, then `gh project item-edit` to `In Progress`, then `gh issue comment 7 --body "planner: Claude; implementer: Codex"`, in that order; exit 0). Add `test_plan_approved_creates_the_issue` (tasks.md with the token placeholder, no `--priority` → 2; tasks.md reading `tracking issue 7` and the literal placeholder elsewhere → the existing branch, no `gh issue create`; with `--priority High` → `gh issue create --title <change> --body-file …proposal.md` answered with `https://github.com/owner/repo/issues/7`, then tasks.md reads `tracking issue 7` and no placeholder, then `item-edit` Planned and Priority High, in that order; a `_Gh` that raises `RuntimeError` on `item-edit` → 1, tasks.md already reads `tracking issue 7` and the message names `set_status.py 7 Planned --priority High`) and `test_existing_tracking_issue` (tasks.md with `tracking issue 7` and `--priority` → 2; without → `item-edit` Planned only, no `gh issue create`). `tests/publisher/test_planning_workflow.py`: `test_rework_verdict` asserts `start_change.py` in the rule and not `grep -q "^approve"`; `test_tasks_of_a_new_change` asserts `rule.index("priority") < rule.index("create_tracking_issue.py")` and `"gh issue create" not in rule`, `"sed -i" not in rule`, `"gh issue develop" not in rule`; `test_plan_approved` asserts `create_tracking_issue.py` in the review entry and `--priority` after it, group 0 names `start_change.py`, `propose run not finished`, the placeholder convention and no `"Planned"` string, no `gh issue view`. Update the module docstrings of `test_delivery_scripts.py` and `test_planning_workflow.py` (v2-2f). Prove RED: `python .agent-process/scripts/check_red.py tests/publisher/test_delivery_scripts.py::test_verdict_is_rework tests/publisher/test_delivery_scripts.py::test_propose_run_stopped_before_its_tail tests/publisher/test_delivery_scripts.py::test_tasks_of_a_new_change_start tests/publisher/test_delivery_scripts.py::test_plan_approved_creates_the_issue tests/publisher/test_delivery_scripts.py::test_existing_tracking_issue tests/publisher/test_planning_workflow.py::test_rework_verdict tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change tests/publisher/test_planning_workflow.py::test_plan_approved` exits 0 (`v2-2d-check-red-test` merged as #145: the script is on `main`); commit `test(implementation): RED for start_change and create_tracking_issue`

## 2. `start_change` and `create_tracking_issue` (D1, D2)

- [x] 2.1 `.agent-process/scripts/start_change.py`: `main(argv)` with `change`, `--planner`, `--implementer` (choices Claude/Codex, required); `from set_status import run_gh, set_status`; `verdict(change_dir)` reads the first non-empty line after `## Verdict` of `architect-review.md`; not starting with `approve` → print `verdict: <line> — apply the findings, re-review (no delivery task runs)`, exit 2; `tracking_issue(tasks_md)` reads the D3 token alone: the token with the placeholder → `None`, `tracking issue (\d+)` → the number, neither → `None` with the convention named (the literal placeholder elsewhere in the file is text) → on `None` print `propose run not finished: run its tail (python .agent-process/scripts/create_tracking_issue.py <change> --priority <High|Medium|Low>) first`, exit 2; `gh issue view N --json projectItems` → an empty `projectItems` (`Status: none`) or `projectItems[0].status.name` ≠ `Planned` → the same line with `Status: <name>`, exit 2; then `gh issue develop -c N --name <change>`, `set_status(N, "In Progress", gh=gh)`, `gh issue comment N --body "planner: <p>; implementer: <i>"`; `RuntimeError` from `gh` → its message, exit 1; `ValueError`/`KeyError` from `set_status` → its message, exit 2 (as `set_status.main` maps them); `main` takes `gh=run_gh` for the tests. Verify `python -m pytest tests/publisher/test_delivery_scripts.py -q -k "verdict_is_rework or stopped_before_its_tail or new_change_start"` green; commit `feat(implementation): start_change — the Group 0 gate, branch, status and provenance as exit codes`
- [x] 2.2 `.agent-process/scripts/create_tracking_issue.py`: `main(argv, gh=run_gh)` with `change`, `--priority` (choices High/Medium/Low, optional); tasks.md carries the D3 token with the placeholder (the same read as `start_change`, not the literal anywhere): no `--priority` → print `priority required: the change has no tracking issue yet`, exit 2; else `gh issue create --title <change> --body-file openspec/changes/<change>/proposal.md` → number = last path segment of the last line of stdout; write the number into tasks.md at once (only the placeholder token of D3, `re.sub`, not every occurrence of the literal) — before `set_status`, so a failure after the create never leaves tasks.md without the number and a re-run never creates a second issue; then `set_status(N, "Planned", gh=gh, priority=P)`; print the URL; tasks.md matches `tracking issue (\d+)`: `--priority` given → print `priority is set at creation; the issue exists`, exit 2; else `set_status(N, "Planned", gh=gh)`; neither → print the convention (the placeholder token of D3 in Group 0), exit 2; `RuntimeError` from `gh` → 1, `ValueError`/`KeyError` from `set_status` → the message, 2 — on the create branch both messages end with the resume `python .agent-process/scripts/set_status.py <number> Planned --priority <P>` (the number is in tasks.md now, and the existing-number branch refuses `--priority`). Verify `python -m pytest tests/publisher/test_delivery_scripts.py -q -k "creates_the_issue or existing_tracking_issue"` green; commit `feat(planning): create_tracking_issue — the propose tail as one command`

## 3. The `tasks` rule names the calls (D3)

- [x] 3.1 `openspec/config.yaml`, rule `tasks`: Group 0 entry becomes `Group 0 — Delivery start: python .agent-process/scripts/start_change.py <change> --planner <carrier of the propose run> --implementer <this carrier> (Claude or Codex; ask when the planner is unknown) — the placeholder token of D3 with the number the propose run wrote into this task: the script reads the verdict of architect-review.md (on rework no delivery task runs: apply the findings, re-review), the Status of the issue, creates the linked branch from origin/main, sets "In Progress" and posts the provenance line; it prints propose run not finished and exits 2 when the tasks still carry the placeholder or the issue is not Planned — nothing is asked and nothing is created here`; the Architect review entry's tail becomes: on `approve` — when the change has no tracking issue, ask the person for its priority (High/Medium/Low), then `python .agent-process/scripts/create_tracking_issue.py <change> --priority "<the answer>"` (creates the issue from proposal.md, sets Planned with the priority, writes the number into tasks.md: Group 0 is how the implementer of another session finds it); when it exists, `python .agent-process/scripts/create_tracking_issue.py <change>` alone (Todo → Planned); only then report the artifacts ready. Group 1 and Deliver untouched. Verify `python -m pytest tests/publisher/test_planning_workflow.py -q` green and `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid; commit `feat(planning): the tasks rule names start_change and create_tracking_issue`

## 4. The record (D3, ADR)

- [ ] 4.1 `.agent-process/docs/architecture/agent-process.md`: §Planning (lines 27–45) — the tail of the propose run is `create_tracking_issue.py <change> [--priority]`; step 3 of the delivery flow (lines 84–99, after `v2-2e-wait-for-pr-checks`) — `start_change.py <change> --planner --implementer` (gate, branch, status, provenance; exit 2 stops the apply); governance items 1 and 5 (lines 253–274) follow. `.agent-process/docs/adr/0027-v2-standards-replace-the-bespoke-control-plane.md`, table "Native alternatives considered": new rows `start_change` → `gh issue develop; gh project item-edit (set_status); gh issue comment` | `each step is native; the gate (verdict, Planned) and the order as exit codes are the script` and `create_tracking_issue` → `gh issue create; gh project item-edit (set_status)` | `gh issue create prints a URL, not JSON; the number into tasks.md and Planned with the priority are the composition`; Deletion condition gains `OpenSpec gains a post-review hook → start_change and create_tracking_issue are deleted`; one bullet after the `v2-2e-wait-for-pr-checks` entry: `Group 0 and the propose tail as scripts (v2-2f-start-change)` with the tracking issue number — the observations of proposal.md in one sentence each (`projectItems` shape; `gh issue develop` base sentence; `gh issue create` prints a URL), what is deleted (five copies of Group 0, `sed -i` in the rule). Verify `python -m pytest tests/agent_process/test_doc_headers.py tests/agent_process/test_doc_links.py tests/agent_process/test_doc_narrative.py tests/agent_process/test_adr_records.py -q` green; commit `docs(implementation): ADR 0027 start_change rows and the delivery flow on start_change`

## 5. Verify

- [ ] 5.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid and `python .agent-process/scripts/ci_check.py` green; `git status --short` shows nothing of this change uncommitted

## 6. Deliver

- [ ] 6.1 `git status --short` empty for this change's files → `python .agent-process/scripts/archive_change.py v2-2f-start-change` (marks its own task, archives, commits, pushes)
- [ ] 6.2 `gh pr create --title "v2-2f-start-change" --body-file <report>` — the change name, the tracking issue as a plain reference (no `Closes`), Why / What, the scenario → test map below, the deferral: the first `start_change` of a real change is the next change's Group 0 (this change's own Group 0 ran the shell form); ends with the Claude Code footer
- [ ] 6.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` after the PR and after every push → `python .agent-process/scripts/wait_for_pr.py <PR>` → apply every unresolved thread, push, re-request, `wait_for_pr.py <PR>` again; a `P0`/`P1` thread the push addressed: `python .agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>` (one thread per rerun cycle, `wait_for_pr.py` between); a `P2`/`P3` thread is answered; a fix that changes a spec goes through a change of its own on the branch; at most three rounds, the fourth leaves the rest to the person with a reply. No tick after the archive; a run interrupted after it continues from `gh pr view v2-2f-start-change`. The person merges

## Scenario → test map

- implementation / Tasks of a new change → `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` and `::test_plan_approved` (task 1.1: the rule names `start_change.py` first and `create_tracking_issue.py` in the tail, no shell steps) and `tests/publisher/test_delivery_scripts.py::test_tasks_of_a_new_change_start` (the three `gh` calls in order on a `Planned` issue)
- implementation / Propose run stopped before its tail → `tests/publisher/test_delivery_scripts.py::test_propose_run_stopped_before_its_tail` (the placeholder, `Todo` and an empty `projectItems` → 2 with the line, no `gh issue develop`)
- implementation / Verdict is rework → `tests/publisher/test_delivery_scripts.py::test_verdict_is_rework` and `tests/publisher/test_planning_workflow.py::test_rework_verdict` (the rule names `start_change.py`, not a `grep`)
- implementation / Blocking thread addressed → `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` (unchanged assertions on the Deliver entry; no script change in this delta)
- implementation / Review fix changes a spec → `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` (unchanged assertion `never a direct edit of openspec/specs/` before `three rounds`)
- planning / Review finding → `tests/publisher/test_planning_workflow.py::test_review_finding` (unchanged)
- planning / Rework verdict → `tests/publisher/test_planning_workflow.py::test_rework_verdict` (task 1.1: `start_change.py` in the rule)
- planning / Plan approved → `tests/publisher/test_planning_workflow.py::test_plan_approved` (task 1.1) and `tests/publisher/test_delivery_scripts.py::test_plan_approved_creates_the_issue` (priority required, `gh issue create`, Planned with priority, the placeholder replaced)
- planning / Existing tracking issue → `tests/publisher/test_delivery_scripts.py::test_existing_tracking_issue` (no create, `--priority` refused, Planned alone)
- planning / Review archives with the change → `tests/publisher/test_planning_workflow.py::test_review_archives_with_the_change` (unchanged)
