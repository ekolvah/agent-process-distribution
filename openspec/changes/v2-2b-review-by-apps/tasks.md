## 0. Delivery start

- [x] 0.1 Gate: `grep -q "^approve" openspec/changes/v2-2b-review-by-apps/architect-review.md` exits 0 (on `rework`: apply the findings, re-review, no delivery task runs) and `gh issue view 130 --json projectItems --jq '.projectItems[0].status.name'` prints `Planned`; otherwise print `propose run not finished: run its tail (the Architect review entry of this rule) first` and stop — nothing is asked and nothing is created here
- [x] 0.2 Branch: `git fetch origin && git checkout main && git pull` → `gh issue develop -c 130 --name v2-2b-review-by-apps`; verify `git branch --show-current` prints the change name
- [x] 0.3 `python .agent-process/scripts/set_status.py 130 "In Progress"` exits 0 (priority High is already set on the issue)
- [x] 0.4 Provenance: `gh issue comment 130 --body "planner: Claude; implementer: <this carrier>"` (Claude or Codex)

## 1. RED first

- [x] 1.1 `tests/publisher/test_reusable_workflows.py`: add `test_agent_review_waits_for_codex_falls_back_to_claude_and_enforces_threads` (scenarios "Valid Codex review", "Codex review absent", "Review event") — the `agent-review` job of `reusable-agent-review.yml` has exactly the steps `Checkout reviewed PR head` (`ref: ${{ github.event.pull_request.head.sha }}`), `Checkout trusted review source` (`repository: ekolvah/agent-process-distribution`, `ref: ${{ github.job_workflow_sha }}`, `path: trusted`), `Wait for the Codex review of the head` (`id: codex`, `continue-on-error: true`, `working-directory: trusted`, `if` contains `github.event_name == 'pull_request'`, `run` contains `request_codex_review.py --wait` and `inputs.codex-timeout-seconds`), `Claude review` (`if` contains `steps.codex.outcome != 'success'` and `github.event_name == 'pull_request'`; `uses` starts with `anthropics/claude-code-action@`; `with.claude_code_oauth_token == "${{ secrets.claude_code_oauth_token }}"`, `with.github_token == "${{ github.token }}"`, `mcp__github_inline_comment__create_inline_comment` in `claude_args`, `--json-schema` absent, the prompt names `trusted/.agent-process/REVIEW_CONTRACT.md`, `untrusted`, `P0`, `Reviewed head SHA` and `Never approve`) and `Enforce unresolved P0/P1 threads` (`if: always()`, `working-directory: trusted`, `run` contains `check_blocking_review_threads.py`); the `workflow_call` input `codex-timeout-seconds` kept; `check_agent_review_outcome` and `STANDARD_REVIEW_PARSER` in no step. Delete `test_agent_review_prompt_points_at_the_contract_rule`, `test_agent_review_keeps_claude_as_fallback_after_manual_codex_request`, `test_agent_review_requires_structured_review_evidence`, `test_valid_codex_evidence_skips_claude_fallback_and_diagnostic`, `test_invalid_codex_evidence_runs_fail_closed_claude_diagnostic_in_the_selected_outcome_checker_root`, `test_diagnostic_capability_is_feature_detected_on_a_default_branch_that_predates_it`, `test_outcome_checker_location_is_feature_detected_on_a_default_branch_that_predates_it`, `test_review_parser_location_is_feature_detected_on_a_default_branch_that_predates_it`, `test_review_contract_location_is_feature_detected_on_a_default_branch_that_predates_it`, `test_blocking_threads_invocation_is_feature_detected_on_a_default_branch_that_predates_it`, `test_agent_review_publishes_fallback_findings_only_when_codex_invalid`; in `test_review_contract_and_principles_stay_coupled_on_narrow_simplicity_triggers` read one clause (`Assign **P0 or P1**`) and assert `BLOCKING` and `deferred-scope` are absent from the contract; in `test_installation_documents_the_caller_workflow_trust_boundary` keep `"Claude fallback carrier"` and add `"P0/P1"` and `"@codex review"`. Extend `test_no_workflow_step_resolves_a_review_thread` (scenario "Required check and threads") to also assert `--classify`, `/replies` and `check_agent_review_outcome` appear in no step
- [x] 1.2 `tests/publisher/test_blocking_review_threads.py` (scenarios "Unresolved blocking thread", "Advisory thread", "Blocking thread resolved"): delete `test_classification_reply_uses_plain_merge_language` and every `classified` assertion; add `test_open_p1_by_the_claude_review_job_is_merge_blocking` (first comment by `github-actions[bot]` with `P1` → in `blocking_threads`) and `test_open_p2_by_the_claude_review_job_is_not_blocking`; add `test_main_fails_on_an_unresolved_p1_without_replying` (monkeypatched `run_gh` records every call: `main` exits 1, stderr contains `unresolved P0/P1 review threads` and the URL, no call contains `/replies`) and `test_main_passes_once_the_p1_thread_is_resolved` (same payload with `isResolved: true` → exit 0, stdout `ok: no unresolved P0/P1 review threads`); `test_the_required_check_never_resolves_a_thread` kept
- [x] 1.3 `tests/publisher/test_request_codex_review.py` (scenarios "Valid Codex review", "Stale Codex review"): keep `test_request_command_posts_the_exact_codex_trigger`, `test_request_command_surfaces_a_github_failure`, `test_module_entry_point_runs_the_cli`; delete the parser tests (`test_standard_codex_*` … `test_timestamp_tie_prefers_blocking_native_evidence`); add `test_wait_is_present_for_a_native_review_on_the_head` (monkeypatched `run_gh` returns a payload with `reviews.nodes == [{author.login: "chatgpt-codex-connector[bot]", commit.oid: head}]` → `main(["--wait", …])` exits 0 and prints `present`), `test_wait_polls_to_the_timeout_for_a_review_of_an_older_head` (only `commit.oid: "older"` → with a fake `clock`/`sleep` the wait polls until `--timeout-seconds`, exits 3, prints `absent after`), `test_wait_is_present_for_the_clean_comment_naming_the_head` (`comments.nodes` with body `**Reviewed commit:** \`<10-hex prefix>\`` by the app → exit 0), `test_wait_reads_nothing_but_presence` (`inspect.getsource(request_codex_review)` contains no `severity`, `finding`, `evidence`)
- [x] 1.4 `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` (scenario "Blocking thread addressed"): replace the `BLOCKING` sentences with `"a `P0`/`P1` thread the push addressed is resolved" in rule` and `"a `P2`/`P3` thread is answered, never resolved by the process" in rule`, `"BLOCKING" not in rule`, `"until v2-4" not in rule`; the ordering (`wait_for_pr.py` < `re-request` < `resolve_review_thread.py` < `three rounds`) and the `gh run rerun` assertion stay in this PR
- [x] 1.5 `tests/publisher/test_resolve_review_thread.py` (scenario "Addressed finding of either reviewer"): add `test_list_blocking_includes_a_p1_by_the_claude_review_job` (payload whose `P1` thread's first comment is by `github-actions[bot]` → in `list_blocking`; a `P2` by it → not)
- [x] 1.6 Run `python -m pytest tests/publisher/test_reusable_workflows.py tests/publisher/test_blocking_review_threads.py tests/publisher/test_request_codex_review.py tests/publisher/test_planning_workflow.py tests/publisher/test_resolve_review_thread.py -q --junitxml=reports/junit.xml` (delete `reports/` afterwards — not tracked); verify `python .agent-process/scripts/check_red.py --report reports/junit.xml <the node ids added or rewritten in 1.1–1.5>` exits 0 (the deleted parser tests are not in the set); commit `test(v2-2b): RED for review by the official apps`

## 2. The review job waits, falls back, enforces

- [x] 2.1 Rewrite `.github/workflows/reusable-agent-review.yml` per design D1 (five steps, input kept, header comment: Codex reviews on its own, Claude when it is silent, the last step fails on an unresolved `P0`/`P1`, nothing parses a review); `.github/workflows/agent-review.yml` unchanged in this PR. Verify `python -m pytest tests/publisher/test_reusable_workflows.py -q` green (`test_callee_schema_matches_local_callers_in_both_directions`, `test_caller_permissions_are_a_superset_of_callee_permissions` included); commit `feat(v2-2b): the review job waits for Codex, falls back to Claude, enforces P0/P1 threads`
- [x] 2.2 `.agent-process/REVIEW_CONTRACT.md` per design D2 (one page: policy source, what to look for, the two §VII triggers in the one `Assign **P0 or P1**` clause, the `P0`–`P3` scale — `P0`/`P1` fail the check —, no re-raise, publication with the label first and `Reviewed head SHA`, never approve/request changes/merge); `AGENTS.md` keeps its link. Verify `python -m pytest tests/publisher/test_reusable_workflows.py -q -k contract` green; commit `docs(v2-2b): the review contract is the reviewer prompt`

## 3. Nothing parses a review

- [x] 3.1 `git rm .agent-process/scripts/check_agent_review_outcome.py tests/publisher/test_agent_review_outcome.py`; `.agent-process/scripts/request_codex_review.py` per design D4: keep `CODEX_REVIEWER`, `REQUEST_BODY`, `request_review`, `--request`; add `_REVIEWS_QUERY`, `codex_reviewed`, `wait_for_codex_review`, `--wait` with `--repo/--pr/--head-sha/--timeout-seconds/--poll-seconds` (exit 0 present, 3 absent, 2 on a GraphQL failure); delete `STANDARD_REVIEW_PARSER`, `_SEVERITIES`, the `check_agent_review_outcome` import, `_finding` … `poll_for_verdict`, `_fetch_*`, `_clean_reaction_*`, `--head-observed-at`, `--reviewer`; docstring: presence-only, never a parse. Edit the one comment line in `open_pr.py` and `verify_pr_link.py` that names the deleted file. Verify `python -m pytest tests/publisher/test_request_codex_review.py tests/agent_process/test_review_credentials.py tests/publisher/test_deferred_scope.py -q` green; commit `refactor(v2-2b): delete the review outcome checker; the Codex read is presence only`
- [x] 3.2 `.agent-process/scripts/check_blocking_review_threads.py` per design D3 (`_REVIEWERS`, no `classified`/marker/`_publish_classifications`, the `P0/P1` messages, docstring); `.agent-process/scripts/resolve_review_thread.py` and `.agent-process/scripts/wait_for_pr.py`: docstring lines only (the check is the reader of both reviewers; the `v2-4` sentence goes). Verify `python -m pytest tests/publisher/test_blocking_review_threads.py tests/publisher/test_resolve_review_thread.py tests/publisher/test_delivery_scripts.py -q` green; commit `refactor(v2-2b): the P0/P1 gate replies to no thread and reads both reviewers`

## 4. Rule and records

- [x] 4.1 `openspec/config.yaml`, Deliver group per design D6, first PR (the request after the PR and after every push, its `(v1, until v2-4; …)` note replaced by what the check does; `P0`/`P1` resolve; `P2`/`P3` answered; the `gh run rerun` clause kept). Verify `python -m pytest tests/publisher/test_planning_workflow.py -q` green; commit `feat(v2-2b): the Deliver group names P0/P1 and P2/P3`
- [x] 4.2 `.agent-process/docs/adr/0027-v2-standards-replace-the-bespoke-control-plane.md`: *Observations from v2-2b* (`v2-2b-review-by-apps`, tracking issue #130) with the questions of design D7, the facts written now (the person's three decisions of 2026-09-17 with the Codex settings' trigger options and why automatic reviews stay off) and placeholders `<observed on the PR of this change>` for the rest; ADR 0015/0022 lines in *More Information* → `v2-2b` with D7's wording; ADR 0020 status line as in D7. `.agent-process/docs/architecture/agent-process-installation.md` step 4: the token is the Claude fallback carrier's credential, used only when the requested Codex review does not arrive; the `@codex review` sentence stays; the check fails on an unresolved `P0/P1` thread and replies to none. Verify `python -m pytest tests/publisher/test_adr_records.py tests/publisher/test_reusable_workflows.py -q -k "adr or installation"` green; commit `docs(v2-2b): observations of review by the apps; installation step 4`

## 5. Verify

- [x] 5.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` reports only the pre-existing failure of the untracked `v2-2-delivery` (a change of #112, not of this PR; none when it is absent) and `python .agent-process/scripts/ci_check.py` green with that directory set aside; `git status --short` shows nothing of this change uncommitted

## 6. Deliver

- [ ] 6.1 `git status --short` empty for this change's files → `python .agent-process/scripts/archive_change.py v2-2b-review-by-apps` (marks its own task, archives, commits, pushes). Nothing is asked here: no setting of either app changes (design, Migration 1)
- [ ] 6.2 `gh pr create --title "v2-2b-review-by-apps" --body-file <report>` — the report names the tracking issue as a plain reference (#130, no `Closes`), Why / What, the scenario → test map below, the second PR that follows; ends with the Claude Code footer
- [ ] 6.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` right after the PR opens, as the rule says → `python .agent-process/scripts/wait_for_pr.py <PR>` (this PR is reviewed by the v1 workflow on `main`; it waits up to 600 s for Codex and falls back to Claude). Then the loop: apply every unresolved thread, push, re-request; a `P0`/`P1` thread the push addressed: `python .agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id>` before the v1 run on that head reaches its last step (later: `gh run rerun <run-id> --failed`); a `P2`/`P3` thread is answered; fill the `<observed on the PR of this change>` placeholders of the ADR observation from this PR in the first push of the loop (`gh api repos/ekolvah/agent-process-distribution/pulls/<PR>/reviews --jq '.[] | .user.login + " " + .commit_id'` and `gh run view <run-id>`: whether the Claude fallback ran, on which head and why — Codex silent, a limit message, an error — and the run's time; the login of Claude's inline comments if it ran); at most three rounds, the fourth leaves the rest to the person with a reply. The person merges. No tick after the archive; a run interrupted after it continues from `gh pr view v2-2b-review-by-apps`

## 7. Second PR: the check follows review events (after the first PR merges)

- [ ] 7.1 `git checkout main && git pull` → `git checkout -b issue-130-review-events` (the linked branch merged with the first PR; this one references #130 in its body). RED: `tests/publisher/test_reusable_workflows.py::test_agent_review_caller_follows_review_events` (the caller `agent-review.yml`'s `on` has `pull_request_review.types == [submitted]` and `pull_request_review_thread.types == [resolved, unresolved]` besides `pull_request`) and, in `test_tasks_of_a_new_change`, `"gh run rerun" not in rule` and `"reaches its last step" not in rule` replacing the re-run ordering; run with `--junitxml`, `check_red` on the two node ids; commit `test(v2-2b): RED for the check following review events`
- [ ] 7.2 `.github/workflows/agent-review.yml` per design D6 (three triggers); `openspec/config.yaml` Deliver group: delete "before the `agent-review` run on that head reaches its last step (… `gh run rerun` …)" — the check re-runs on the resolve. Verify `python -m pytest tests/publisher/test_reusable_workflows.py tests/publisher/test_planning_workflow.py -q` green, `python .agent-process/scripts/ci_check.py` green; commit `feat(v2-2b): the review check re-runs on review events`
- [ ] 7.3 `gh pr create --title "v2-2b-review-by-apps: review events" --body-file <report>` (references #130; names the observation it makes) → `python .agent-process/scripts/wait_for_pr.py <PR>` → `gh pr checks <PR>` after Codex's review of the head: a second `agent-review` run on the same head is the observation that a review-event run is listed for the head; write it into ADR 0027's observation in the loop's first push (and, when a `P0`/`P1` thread is resolved in this loop, that the check went green without a re-run). If no review-event run is listed for the head: revert the caller change in this PR, keep the rule's re-run clause, record it — the person decides on the PR. Loop as in 6.3 without the re-run clause; the person merges

## Scenario → test map

- review-and-merge / Valid Codex review → `tests/publisher/test_reusable_workflows.py::test_agent_review_waits_for_codex_falls_back_to_claude_and_enforces_threads` (task 1.1), `tests/publisher/test_request_codex_review.py::test_wait_is_present_for_a_native_review_on_the_head` (task 1.3)
- review-and-merge / Stale Codex review → `tests/publisher/test_request_codex_review.py::test_wait_polls_to_the_timeout_for_a_review_of_an_older_head` (task 1.3)
- review-and-merge / Codex review absent → `tests/publisher/test_reusable_workflows.py::test_agent_review_waits_for_codex_falls_back_to_claude_and_enforces_threads` (task 1.1)
- review-and-merge / Review event → `tests/publisher/test_reusable_workflows.py::test_agent_review_waits_for_codex_falls_back_to_claude_and_enforces_threads` (task 1.1, the `if` guards), `::test_agent_review_caller_follows_review_events` (task 7.1)
- review-and-merge / Unresolved blocking thread → `tests/publisher/test_blocking_review_threads.py::test_main_fails_on_an_unresolved_p1_without_replying`, `::test_open_p1_by_the_claude_review_job_is_merge_blocking` (task 1.2)
- review-and-merge / Advisory thread → `tests/publisher/test_blocking_review_threads.py::test_open_p2_by_the_claude_review_job_is_not_blocking` (task 1.2), `::test_open_p2_is_explicitly_nonblocking` (existing)
- review-and-merge / Blocking thread resolved → `tests/publisher/test_blocking_review_threads.py::test_main_passes_once_the_p1_thread_is_resolved` (task 1.2)
- review-and-merge / Required check and threads → `tests/publisher/test_reusable_workflows.py::test_no_workflow_step_resolves_a_review_thread` (task 1.1), `tests/publisher/test_blocking_review_threads.py::test_the_required_check_never_resolves_a_thread` (existing)
- review-and-merge / Addressed finding of either reviewer → `tests/publisher/test_resolve_review_thread.py::test_list_blocking_includes_a_p1_by_the_claude_review_job` (task 1.5)
- review-and-merge / Contract and principles → `tests/publisher/test_reusable_workflows.py::test_review_contract_and_principles_stay_coupled_on_narrow_simplicity_triggers` (task 1.1)
- implementation / Tasks of a new change → `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` (existing)
- implementation / Propose run stopped before its tail → `tests/publisher/test_planning_workflow.py::test_plan_approved` (existing)
- implementation / Blocking thread addressed → `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` (tasks 1.4, 7.1)
