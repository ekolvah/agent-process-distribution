## 0. Delivery start

- [x] 0.1 Gate: `grep -q "^approve" openspec/changes/v2-2e-wait-for-pr-checks/architect-review.md` exits 0 (on `rework`: apply the findings, re-review, no delivery task runs) and `gh issue view 143 --json projectItems --jq '.projectItems[0].status.name'` prints `Planned` (tracking issue 143); otherwise print `propose run not finished: run its tail (the Architect review entry of this rule) first` and stop — nothing is asked and nothing is created here
- [x] 0.2 Branch: `git fetch origin && git checkout main && git pull` → `gh issue develop -c 143 --name v2-2e-wait-for-pr-checks`; verify `git branch --show-current` prints the change name
- [x] 0.3 `python .agent-process/scripts/set_status.py 143 "In Progress"` exits 0 (the priority is already set on the issue)
- [x] 0.4 Provenance: `gh issue comment 143 --body "planner: Claude; implementer: <this carrier>"` (Claude or Codex)

## 1. RED first

- [x] 1.1 `tests/publisher/test_delivery_scripts.py` (D2): `_Sequence` becomes the fake `gh(cmd)` returning `subprocess.CompletedProcess` — for `gh pr checks … --json …` the next of a list of `(rc, stdout, stderr)` (the last one repeats), counted in `polls`; `gh repo view` and `gh api graphql` as today, the threads payload carrying `url` in place of `headRefOid`; `_checks(*(name, bucket))` builds a passing `(0, json, "")` answer with `link` `https://ci.test/<name>`, `_EMPTY = (1, "", "no checks reported on the 'x' branch\n")`. Rewrite `test_pending_review`: `pending` then `fail` → 1, `failed: agent-review` in out, `polls == 2`; `cancel` → 1; threads → 1 and `docs/a.md`; all `pass` → 0 and `clean:` and the URL in out; `pending` on every read with `timeout=120` → 3 with `timeout` and `agent-review` in out; a read `(1, "", "HTTP 401")` → `pytest.raises(RuntimeError, match="401")`. Add `test_empty_rollup_after_push`: `_EMPTY` then pass → 0, `sleep` called once with 30, `polls == 2`; `_EMPTY` on every read with `timeout=120` → 3 and `no checks` in out. Delete the settling and head cases and `_rollup`; `test_none_capture_is_an_error` stays. Module docstring names v2-2e. Verify `python .agent-process/scripts/check_red.py tests/publisher/test_delivery_scripts.py::test_pending_review tests/publisher/test_delivery_scripts.py::test_empty_rollup_after_push` exits 0; commit `test(implementation): RED — wait_for_pr reads gh pr checks --json until nothing is pending`

## 2. `wait_for_pr` on `gh pr checks --json` (D1)

- [x] 2.1 `.agent-process/scripts/wait_for_pr.py`: `run_gh(cmd) -> subprocess.CompletedProcess[str]` (`capture_output=True, encoding="utf-8"`; a `None` capture → `RuntimeError("… broken capture …")`; no raise on rc ≠ 0); `_json(gh, cmd)` raises `RuntimeError` on rc ≠ 0 with the stderr, then parses; `_checks(gh, pr) -> list[dict] | None` runs `gh pr checks <pr> --json name,bucket,link`: rc ≠ 0 with `no checks reported` in stderr → `None`, other rc ≠ 0 → `RuntimeError` with the stderr, else the parsed list; `wait_for_pr(pr, *, gh=run_gh, clock=time.monotonic, sleep=time.sleep, timeout=DEFAULT_TIMEOUT)`: `deadline = clock() + timeout`, `last = None`; loop: `checks = _checks(gh, pr)`; `pending` = names with bucket `pending`; `checks and not pending` → break; `waiting = ", ".join(pending) or "no checks reported"`; `clock() + POLL_SECONDS > deadline` → print `timeout after <int(timeout)>s: <waiting>`, return 3; `waiting != last` → print `waiting: <waiting>`, `last = waiting`; `sleep(POLL_SECONDS)`. After the loop: `_THREADS_QUERY` asks `url` where it asked `headRefOid`, `_unresolved_threads` returns `(url, threads)`; print `failed: <name> <link>` for every bucket not in `{"pass", "skipping"}` (`link` via `.get`, default `""`), the `unresolved:` lines as today, `clean: <N> checks green, no unresolved threads (<url>)` when nothing; return 1 or 0. Delete `_GREEN`, `_concluded`, `_failed`, `_name`, `_url`, the `gh pr view` poll, the settling and head-comparison code; keep `POLL_SECONDS = 30`, `DEFAULT_TIMEOUT`, `main` (exit 2 on `RuntimeError`). Module docstring: the read, why `--json` and not `--watch` (checks.go lines 81, 184–191, 218, 228–237, 248–252, 303; aggregate.go buckets and the latest run per name), the exit codes. Verify `python -m pytest tests/publisher/test_delivery_scripts.py -q` green; commit `refactor(implementation): wait_for_pr reads gh pr checks --json until nothing is pending`

## 3. The record

- [x] 3.1 `.agent-process/docs/architecture/agent-process.md` step 3 of the delivery flow (the `wait_for_pr.py <PR>` mention, line 95): one clause — it reads `gh pr checks --json` until nothing is pending, retrying the empty rollup, then the threads. `.agent-process/docs/adr/0027-v2-standards-replace-the-bespoke-control-plane.md`: the `wait_for_pr` row of "Native alternatives considered" → `gh pr checks --watch`; `gh pr checks --json` | `--watch exits 1 on the empty rollup after a push as on a failure, refuses --json and leaves on Pending == 0, so a loop of our own remains — that loop reads --json (the buckets and the latest run per name are gh's) every 30 s; review threads are GraphQL-only (v2-2e)`; deletion condition → `gh pr checks --watch learns the empty rollup and review threads → wait_for_pr is deleted`; one bullet in "More Information" after the `check_red` entry: `wait_for_pr on gh pr checks --json (issue 143, v2-2e-wait-for-pr-checks)` — the observations of proposal.md one sentence each (checks.go lines 81, 184–191, 218, 228–237, 248–252, 303; `commits(last: 1)`; aggregate.go buckets and lines 96–120; `gh pr checks 145 --json` exit 0 and its fields; PR 137's one `agent-review` entry after the rerun), why `--watch` was ruled out (design.md D1), what is deleted (the `gh pr view --json statusCheckRollup` poll and its sorting, the settling guard, the head comparison) and what the script stops proving (design.md D1). Verify `python -m pytest tests/agent_process -q` green; commit `docs(implementation): ADR 0027 wait_for_pr row and record; delivery flow on gh pr checks --json`

## 4. Verify

- [x] 4.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid and `python .agent-process/scripts/ci_check.py` green; `git status --short` shows nothing of this change uncommitted

## 5. Deliver

- [x] 5.1 `git status --short` empty for this change's files → `python .agent-process/scripts/archive_change.py v2-2e-wait-for-pr-checks` (marks its own task, archives, commits, pushes)
- [ ] 5.2 `gh pr create --title "v2-2e-wait-for-pr-checks" --body-file <report>` — the change name, issue 143 as a plain reference (no `Closes`), Why / What, the scenario → test map below, the note that task 5.3 is the first live run of the rewritten script; ends with the Claude Code footer
- [ ] 5.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` after the PR and after every push → `python .agent-process/scripts/wait_for_pr.py <PR>` (the rewritten script is on the branch: its first live run is this PR) → apply every unresolved thread, push, re-request, `wait_for_pr.py <PR>` again; a `P0`/`P1` thread the push addressed: `python .agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>` (one thread per rerun cycle, `wait_for_pr.py` between); a `P2`/`P3` thread is answered; a fix that changes a spec goes through a change of its own on the branch. Issue 146 binds the fixer: a finding is closed by its class — the fix names the invariant the finding violates, enumerates the other inputs that violate it from `gh pr checks --help` and the sources cited in design.md, states what each added switch takes away, and the RED test covers the class, not the reviewer's example; a fix that changes a design decision amends the archived `design.md` beside that decision and the scenario → test map of the archived `tasks.md` in the same push. At most three rounds, the fourth leaves the rest to the person with a reply. No tick after the archive; a run interrupted after it continues from `gh pr view v2-2e-wait-for-pr-checks`. The person merges

## Scenario → test map

- implementation / Pending review → `tests/publisher/test_delivery_scripts.py::test_pending_review` (task 1.1: `pending` then `fail` → 1, `cancel` → 1, threads → 1, clean → 0, `pending` past the deadline → 3 naming the check, a `gh` failure → `RuntimeError`; PR 147 round 1: a timeout that is not a multiple of the interval is waited through; round 2: a concluded set is trusted once two reads agree — a late-attaching check is waited on)
- implementation / Empty rollup after a push → `tests/publisher/test_delivery_scripts.py::test_empty_rollup_after_push` (the `no checks reported` read → one sleep, then clean; the read that never fills → 3); the live wait on a real head → n/a: task 5.3 runs the script on this PR, recorded by the PR
