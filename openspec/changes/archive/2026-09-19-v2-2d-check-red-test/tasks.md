## 0. Delivery start

- [x] 0.1 Gate: `grep -q "^approve" openspec/changes/v2-2d-check-red-test/architect-review.md` exits 0 (on `rework`: apply the findings, re-review, no delivery task runs) and `gh issue view 132 --json projectItems --jq '.projectItems[0].status.name'` prints `Planned`; otherwise print `propose run not finished: run its tail (the Architect review entry of this rule) first` and stop — nothing is asked and nothing is created here
- [x] 0.2 Branch: `git fetch origin && git checkout main && git pull` → `gh issue develop -c 132 --name v2-2d-check-red-test`; verify `git branch --show-current` prints the change name
- [x] 0.3 `python .agent-process/scripts/set_status.py 132 "In Progress"` exits 0 (the priority is already set on the issue)
- [x] 0.4 Provenance: `gh issue comment 132 --body "planner: Claude; implementer: <this carrier>"` (Claude or Codex)

## 1. RED first

- [x] 1.1 `tests/publisher/test_delivery_scripts.py`: rewrite `test_behavioural_change` — a fake runner `runner.py` in `tmp_path` (finds the `--junitxml=` argument in its argv, writes the RED or GREEN fixture XML there — chosen by an env var the test sets — and writes its argv to `argv.txt`); `check_red.main(["--test", f"{sys.executable} {runner}", "tests/publisher/test_x.py::test_a"])` returns for RED and raises `SystemExit(1)` for GREEN; `argv.txt` shows `--tb=no`, one `--junitxml=` argument and the node id last; `--report` raises `SystemExit(2)` (argparse: the flag is gone; the default runner is exercised live by task 2.1, not spawned in the suite). Move `test_class_scoped_node_id` and `test_parametrized_node_id` to the same fake runner via `--test`. `tests/publisher/test_planning_workflow.py`: `test_tasks_of_a_new_change` asserts `check_red.py --test` in the rule and `"AGENTS.md" not in rule`, `"--report" not in rule`. Update the module docstring of `test_delivery_scripts.py` (v1 → v2-2d). Run `python -m pytest --junitxml=.pytest-report.xml tests/publisher/test_delivery_scripts.py tests/publisher/test_planning_workflow.py -q`, verify `python .agent-process/scripts/check_red.py --report .pytest-report.xml tests/publisher/test_delivery_scripts.py::test_behavioural_change tests/publisher/test_delivery_scripts.py::test_class_scoped_node_id tests/publisher/test_delivery_scripts.py::test_parametrized_node_id tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` exits 0; commit `test(implementation): RED for check_red --test`

## 2. `check_red --test` (D1)

- [x] 2.1 `.agent-process/scripts/check_red.py`: argparse gains `--test` (help: the runner command; default `python -m pytest`) and loses `--report`; `main` builds `cmd = (shlex.split(args.test, posix=os.name != "nt") if args.test else [sys.executable, "-m", "pytest"]) + ["--tb=no", f"--junitxml={report}", *node_ids]` in a `TemporaryDirectory`, runs it with `encoding="utf-8"` (a `None` capture is an error), then `evaluate_report` over `_select_cases(report, node_ids)` — the one evaluation path; exit 0/1/2 as today; the module docstring names `--test` and the report as the script's own. `AGENTS.md` lines 37–39: the test-runner paragraph goes (the sentence on `None` captures stays where it is). `.gitignore` lines 13–14: the `.pytest-report.xml` entry goes, and the `.pytest-report.xml` task 1.1 wrote is deleted (an untracked file would make `archive_change` refuse the worktree). Verify `python -m pytest tests/publisher/test_delivery_scripts.py -q -k "behavioural_change or scoped_node_id or parametrized_node_id"` green and `python .agent-process/scripts/check_red.py tests/publisher/test_delivery_scripts.py::test_behavioural_change` exits 1 (green test → not RED, the default runner ran); commit `feat(implementation): check_red runs the runner given by --test; no declared report path`

## 3. The `tasks` rule names the call (D2)

- [x] 3.1 `openspec/config.yaml`, rule `tasks`, Group 1: `python .agent-process/scripts/check_red.py --test "<runner command>" <node ids>` (the runner of this repository is the default, so `--test` is omitted here), no `AGENTS.md` declaration, no report path; Groups 0 and Deliver untouched. Verify `python -m pytest tests/publisher/test_planning_workflow.py -q` green and `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid; commit `feat(planning): the tasks rule names check_red --test`

## 4. The record

- [x] 4.1 `.agent-process/docs/architecture/agent-process.md`, step 3 of the delivery flow (lines 84–94): `check_red.py --test` runs the runner and reads its own report — no declaration in `AGENTS.md`. `.agent-process/docs/adr/0027-v2-standards-replace-the-bespoke-control-plane.md`, table "Native alternatives considered": `check_red` row → `pytest --junitxml` | `the script runs the runner and reads the report it wrote; what remains is the per-test RED verdict, which no runner prints`; one bullet after the issue-131 entry: `check_red on the runner's own report (issue 132, v2-2d-check-red-test)` — the pytest `--help` observation, what is deleted (`--report`, the AGENTS.md declaration, the `.gitignore` entry), and the split of step 2d into `v2-2d`/`v2-2e`/`v2-2f` with its reason (one sentence). Verify `python -m pytest tests/agent_process/test_doc_headers.py tests/agent_process/test_doc_links.py tests/agent_process/test_doc_narrative.py tests/agent_process/test_adr_records.py -q` green; commit `docs(implementation): ADR 0027 check_red row; delivery flow on check_red --test`

## 5. Verify

- [x] 5.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid and `python .agent-process/scripts/ci_check.py` green; `git status --short` shows nothing of this change uncommitted

## 6. Deliver

- [x] 6.1 `git status --short` empty for this change's files → `python .agent-process/scripts/archive_change.py v2-2d-check-red-test` (marks its own task, archives, commits, pushes)
- [ ] 6.2 `gh pr create --title "v2-2d-check-red-test" --body-file <report>` — the change name, issue 132 as a plain reference (no `Closes`), Why / What, the scenario → test map below; ends with the Claude Code footer
- [ ] 6.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` after the PR and after every push → `python .agent-process/scripts/wait_for_pr.py <PR>` → apply every unresolved thread, push, re-request, `wait_for_pr.py <PR>` again; a `P0`/`P1` thread the push addressed: `python .agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>` (one thread per rerun cycle, `wait_for_pr.py` between); a `P2`/`P3` thread is answered; a fix that changes a spec goes through a change of its own on the branch; at most three rounds, the fourth leaves the rest to the person with a reply. No tick after the archive; a run interrupted after it continues from `gh pr view v2-2d-check-red-test`. The person merges

## Scenario → test map

- implementation / Behavioural change → `tests/publisher/test_delivery_scripts.py::test_behavioural_change` (task 1.1: the fake runner receives `--junitxml=` and the node id, RED → 0, GREEN → 1)
- implementation / Runner given → the same test (task 1.1: `--test "<python> runner.py"` is the command run; `--report` is refused) and `test_class_scoped_node_id`, `test_parametrized_node_id` through `--test`; the rule text → `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` (`check_red.py --test`, no `AGENTS.md`)
