## 0. Delivery start

- [x] 0.1 Gate: `grep -q "^approve" openspec/changes/planning-rules-review-rounds/architect-review.md` exits 0 (on `rework`: apply the findings, re-review, no delivery task runs) and `gh issue view 146 --json projectItems --jq '.projectItems[0].status.name'` prints `Planned`; otherwise print `propose run not finished: run its tail (the Architect review entry of this rule) first` and stop — nothing is asked and nothing is created here
- [x] 0.2 Branch: `git fetch origin && git checkout main && git pull` → `gh issue develop -c 146 --name planning-rules-review-rounds`; verify `git branch --show-current` prints the change name
- [x] 0.3 `python .agent-process/scripts/set_status.py 146 "In Progress"` exits 0 (priority High is already set on the issue)
- [x] 0.4 Provenance: `gh issue comment 146 --body "planner: Claude; implementer: <this carrier>"` (Claude or Codex)

## 1. RED first

- [x] 1.1 `tests/publisher/test_planning_workflow.py`: four tests, one per scenario (design.md D6) — `test_replaced_input_designed` (`rules.design` joined: `replaces a project-declared input`, `failure modes`, `stops proving`, `which script, which run, on which head`, `not a role` in it; and `design` in the config's rule keys); `test_untraceable_catcher` (the Architect review entry: `without the list` and `cannot trace` in it, both after `is a finding`); `test_design_decision_changed_at_review` (the Deliver entry: `amends the archived `design.md`` after `never a direct edit of `openspec/specs/`` and before `three rounds`); `test_finding_closed_by_its_class` (the Deliver entry: `closed by its class`, `invariant`, `takes away`, `not the reviewer's example` in it, each after `amends the archived` and before `three rounds`). Verify `python .agent-process/scripts/check_red.py tests/publisher/test_planning_workflow.py::test_replaced_input_designed tests/publisher/test_planning_workflow.py::test_untraceable_catcher tests/publisher/test_planning_workflow.py::test_design_decision_changed_at_review tests/publisher/test_planning_workflow.py::test_finding_closed_by_its_class` exits 0; commit `test(planning): RED for the replaced-input, archived-amendment and fix-by-class rules`

## 2. The design rule lists failure modes and catchers (D1)

- [x] 2.1 `openspec/config.yaml`: a new `design:` key under `rules:` (between `proposal` and `tasks`) with one bullet — `A design that replaces a project-declared input with one the caller supplies, or drops a guard, lists beside that decision the failure modes of the new input (when one is replaced), what the component stops proving, and for each lost proof the delivery-flow step that catches it — which script, which run, on which head — not a role or the platform in general.` Verify `python -m pytest tests/publisher/test_planning_workflow.py -q -k "replaced_input_designed or roles_and_carriers"` green and `npx -y @fission-ai/openspec@1.13.0 instructions design --change planning-rules-review-rounds --json` carries `failure modes`; commit `feat(planning): the design rule lists the failure modes and catchers of a replaced input`

## 3. The architect review has the two findings (D4)

- [x] 3.1 `openspec/config.yaml`, `rules.tasks`, Architect review entry: after `(no reference page, run id or command output beside it, and no pointer to one on record)` add `, a replaced input or a dropped guard without the list of the `design` rule, and a catcher the review cannot trace to a step the flow reaches in the described case`. Verify `python -m pytest tests/publisher/test_planning_workflow.py -q` green (`test_untraceable_catcher`, `test_asserted_platform_fact`, `test_review_finding` with it); commit `feat(planning): the architect review finds a missing failure-mode list and an untraceable catcher`

## 4. The Deliver entry: amendment in the same push, fix by class (D2, D3)

- [x] 4.1 `openspec/config.yaml`, `rules.tasks`, Deliver group: replace ` —, repeat at most three rounds; the fourth leaves the rest to the person with a reply.` (source lines 97–99 of `config.yaml`; the anchor spans two line breaks, the last after `the rest to the`) with ` —; a fix that changes a design decision amends the archived `design.md` beside that decision and the scenario → test map of the archived `tasks.md` in the same push; a finding on what a script does is closed by its class (the invariant it violates, the other inputs that violate it from the tool's own documentation, what each added switch takes away, a RED test of the class, not the reviewer's example); repeat at most three rounds; the fourth leaves the rest to the person with a reply.` (`The person merges.` follows unchanged). Verify `python -m pytest tests/publisher/test_planning_workflow.py -q` green (`test_design_decision_changed_at_review`, `test_finding_closed_by_its_class`, `test_tasks_of_a_new_change` with it) and `python -c "import yaml; yaml.safe_load(open('openspec/config.yaml', encoding='utf-8'))"` exits 0; commit `feat(delivery): a design change at review amends the archive; a finding is closed by its class`

## 5. The record (D3, D5)

- [x] 5.1 `.agent-process/docs/architecture/agent-process.md`: Planning — the `proposal` rule's summary in parentheses is followed by `, the `design` rule (a replaced input lists its failure modes; a replaced input or a dropped guard lists the proofs lost and the delivery-flow step that catches each)` and the finding list gains `an untraceable catcher`; the `fixer` row of the roles table: `Minimal correction with passing relevant checks; a finding on what a script does is closed by its class; a design decision changed at review is amended in the archived change by the same push`. `.agent-process/docs/adr/0027-v2-standards-replace-the-bespoke-control-plane.md`: one bullet after the `wait_for_pr` entry, five lines at most — `Planning rules from the review rounds of PR 145 and 147 (issue 146, planning-rules-review-rounds)`: the three causes, one clause each, pointing at the two entries above; the catcher example of PR 147 (the next `wait_for_pr` after a clean verdict — none); "archive after the review" rejected (the reviewed head would lack the delta under `openspec/specs/`); no new script; deletion condition none. Nothing of the two PRs is re-narrated. Verify `python -m pytest tests/agent_process/test_doc_headers.py tests/agent_process/test_doc_links.py tests/agent_process/test_doc_narrative.py -q` green (no `#N` as a sentence member); commit `docs(planning): ADR 0027 entry, Planning section and fixer row for the review-round rules`

## 6. Verify

- [x] 6.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid and `python .agent-process/scripts/ci_check.py` green; `git status --short` shows nothing of this change uncommitted

## 7. Deliver

- [x] 7.1 `git status --short` empty for this change's files → `python .agent-process/scripts/archive_change.py planning-rules-review-rounds` (marks its own task, archives, commits, pushes)
- [ ] 7.2 `gh pr create --title "planning-rules-review-rounds" --body-file <report>` — the change name, issue 146 as a plain reference (no `Closes`), Why / What, the scenario → test map below, no deferral; ends with the Claude Code footer
- [ ] 7.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` after the PR and after every push → `python .agent-process/scripts/wait_for_pr.py <PR>` → apply every unresolved thread, push, re-request, `wait_for_pr.py <PR>` again — it returns on the concluded check of the head, Codex's or the fallback's —; a `P0`/`P1` thread the push addressed: `python .agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>` (`--list` prints the open ones; the script refuses while the head's run is running, resolves, re-runs that run and replies); a `P2`/`P3` thread is answered, never resolved by the process; a fix that changes a spec goes through a change of its own on the branch (`npx -y @fission-ai/openspec@1.13.0 new change <name>`, the delta, `validate --strict`, `archive_change.py <name>`), never a direct edit of `openspec/specs/`; a fix that changes a design decision amends the archived `design.md` beside it and the map of the archived `tasks.md` in the same push; a finding on what a script does is closed by its class (the invariant, the other violating inputs from the tool's documentation, what each switch takes away, a RED test of the class); at most three rounds, the fourth leaves the rest to the person with a reply. The person merges. No tick after the archive; a run interrupted after it continues from `gh pr view planning-rules-review-rounds`

## Scenario → test map

- planning / Replaced input designed → `tests/publisher/test_planning_workflow.py::test_replaced_input_designed` (task 1.1: the `design` rule the propose run loads names the trigger, the three lists and the exclusion); the planner's compliance on a given change → n/a: judgement of the propose run, read by the architect review (next scenario)
- planning / Untraceable catcher → `tests/publisher/test_planning_workflow.py::test_untraceable_catcher` (task 1.1: the Architect review entry names both findings); the reviewer's trace on a given change → n/a: the subagent's judgement, exercised on every propose run, not a script
- implementation / Design decision changed at review → `tests/publisher/test_planning_workflow.py::test_design_decision_changed_at_review` (task 1.1: the Deliver entry names the amendment between the spec-changing-fix sentence and the round limit); the fixer's amendment on a given PR → n/a: an edit of the archived change read by the PR review, not a script
- implementation / Finding closed by its class → `tests/publisher/test_planning_workflow.py::test_finding_closed_by_its_class` (task 1.1: the Deliver entry names the trigger — a finding on what a script does —, the invariant, the enumeration, what a switch takes away, the RED test of the class); whether a given fix closed its class → n/a: judgement of the fixer, read by the reviewer of the next head
- implementation / Tasks of a new change, Propose run stopped before its tail, Verdict is rework, Blocking thread addressed, Review fix changes a spec → unchanged scenarios of the modified requirement, already proved by `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change` and `::test_plan_approved`; task 4.1 keeps them green
