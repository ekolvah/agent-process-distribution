## 0. Delivery start

- [x] 0.1 Run `python skills/agent-process/scripts/start_change.py release-please --planner Claude --implementer <Claude|Codex: the carrier of this apply run>` for tracking issue 197. Verify that it reads the approved review, creates the linked branch from `origin/main`, sets In Progress, and posts the provenance line before any other work

## 1. RED first

- [x] 1.1 Rewrite `tests/publisher/test_plugin.py::test_version_drift`: read the version of `.release-please-manifest.json` (key `"."`); assert `plugin.json` `version`, `marketplace.json` `plugins[0].version` and `init.VERSION` equal it; assert the `extra-files` of `release-please-config.json` are exactly the two JSON files with jsonpaths `$.version` and `$.plugins[0].version` plus `skills/agent-process/scripts/init.py`, and that the `VERSION` line of `init.py` carries `x-release-please-version`. Run `python skills/agent-process/scripts/check_red.py tests/publisher/test_plugin.py::test_version_drift` and verify it fails in the test body because the manifest is absent; commit as `test(publisher): version places follow the release manifest`
- [x] 1.2 Add `tests/publisher/test_reusable_workflows.py::test_pr_title_requires_a_conventional_commit_type`: `pr-title.yml` runs on `pull_request` with types exactly `opened`, `edited`, `synchronize`, `reopened`; it has one job keyed `pr-title` with `permissions` `pull-requests: read`; its step uses `amannn/action-semantic-pull-request@v6`, and its `types` input, split by lines, is exactly `feat`, `fix`, `docs`, `test`, `refactor`, `chore`. Run `python skills/agent-process/scripts/check_red.py tests/publisher/test_reusable_workflows.py::test_pr_title_requires_a_conventional_commit_type` and verify it fails in the test body because the workflow is absent; commit as `test(publisher): the PR title check requires a Conventional Commit type`

## 2. Installer tests follow the current release (D5)

- [x] 2.1 In `tests/publisher/init_harness.py` define `CURRENT` from `init.VERSION` and `OTHER` as its next minor; build `STUB` and `CHECK_COMMAND` from them. In `conftest.py` tag `v{CURRENT}` and `v{OTHER}`. Replace every release literal `2.0.0`/`2.1.0` in `test_init.py`, `test_init_config.py` and `test_init_remote.py` with `CURRENT`/`OTHER`; leave `test_skill_check.py` as is. Verify `python -m pytest tests/publisher -q` passes except `test_version_drift`
- [x] 2.2 Prove the class: set `2.1.0` in the two JSON files, `init.VERSION` and a temporary manifest, run `python -m pytest tests/publisher -q`, verify it passes except the config assertion of `test_version_drift`, then restore with `git checkout -- .claude-plugin skills` and delete the temporary manifest. Commit as `test(publisher): installer tests follow init.VERSION`

## 3. release-please (D1, D2, D6)

- [x] 3.1 Add `release-please-config.json` and `.release-please-manifest.json` (`{".": "2.0.0"}`) per D1, append `# x-release-please-version` to the `VERSION` line of `init.py`, and add `.github/workflows/release-please.yml`: `on: push: branches: [main]`, permissions `contents`, `issues`, `pull-requests: write`, one step `googleapis/release-please-action@v4` with `token: ${{ secrets.RELEASE_PLEASE_TOKEN }}`. Verify `python -m pytest tests/publisher -q` passes; commit as `feat(release): cut releases with release-please`
- [x] 3.2 Push the branch and run `npx -y release-please@17 release-pr --repo-url=ekolvah/agent-process-distribution --target-branch=release-please --token="$(gh auth token)" --dry-run --debug`. Verify that it reads the branch's config, proposes `2.1.0`, and lists exactly `CHANGELOG.md`, `.release-please-manifest.json` and the three version places as the files it would change; keep the output lines for the PR report. A different file list stops delivery: report it to the person instead of adapting the test
- [x] 3.3 Add `.github/workflows/pr-title.yml` per D7: `on: pull_request: types: [opened, edited, synchronize, reopened]`, one job `pr-title` with `permissions: pull-requests: read` and one step `amannn/action-semantic-pull-request@v6`, `env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}`, `with: types:` the six types, one per line. Verify `python -m pytest tests/publisher/test_reusable_workflows.py::test_pr_title_requires_a_conventional_commit_type -q` passes; commit as `ci: require a Conventional Commit type in the PR title`

## 4. Procedure and record (D3, D4, D7)

- [x] 4.1 In `skills/agent-process/SKILL.md`: in Tasks item 5, the Deliver task names `gh pr create --title "<type>: <change>"` with the Conventional Commit type the planner chose — `feat` or `fix` when behaviour changes, otherwise `docs`, `test`, `refactor` or `chore`; in Delivery, change the command to `gh pr create --title "<type>: <change>"`. Verify `git diff origin/main -- skills/agent-process/SKILL.md` touches only those two places; commit as `docs(skill): title the PR with its Conventional Commit type`
- [x] 4.2 Add `.agent-process/docs/adr/0030-releases-go-through-release-please.md` from `template.md`: the decision D1–D4 and D7, a "Native alternatives considered" section, the deletion condition (remove the PAT when the GitHub App or workflow-token route is adopted; remove release-please if the plugin cache stops keying by version), the person's steps to cut a release (link the issue, request `@codex review`, merge the release PR), and Confirmation naming `test_version_drift`. Verify `python .agent-process/scripts/ci_check.py` passes the document guard; commit as `docs(adr): record releases through release-please`

## 5. Verify

- [x] 5.1 Run `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` and verify that every change and spec passes
- [x] 5.2 Run `python .agent-process/scripts/ci_check.py` and verify that it passes

## 6. Deliver

- [x] 6.1 With a clean worktree, run `python skills/agent-process/scripts/archive_change.py release-please`. Verify that it archives the two deltas into `openspec/specs/`, commits, and pushes the branch
- [ ] 6.2 Run `gh pr create --title "feat: release-please" --body-file <report>`. The report names the tracking issue of task 0.1 as a plain reference (never `Closes`), carries the scenario → test map and the dry-run lines of task 3.2, and lists the person's one-time actions before merge: create a fine-grained PAT for this repository with Contents and Pull requests read/write and run `gh secret set RELEASE_PLEASE_TOKEN`; set the squash title with `gh api -X PATCH repos/ekolvah/agent-process-distribution -f squash_merge_commit_title=PR_TITLE -f squash_merge_commit_message=COMMIT_MESSAGES -F allow_merge_commit=false -F allow_rebase_merge=false`; once `pr-title` has reported on this PR, create the ruleset of D7 with `gh api -X POST repos/ekolvah/agent-process-distribution/rulesets --input -` fed `{"name":"pr-title","target":"branch","enforcement":"active","conditions":{"ref_name":{"include":["~DEFAULT_BRANCH"],"exclude":[]}},"rules":[{"type":"required_status_checks","parameters":{"strict_required_status_checks_policy":false,"required_status_checks":[{"context":"pr-title","integration_id":15368}]}}]}`; after merge, link the tracking issue to the release PR, request `@codex review`, and merge it to cut `v2.1.0`
- [ ] 6.3 Observe the title check on this PR: run `gh pr edit <PR> --title "release-please"`, wait for the `pr-title` run of the `edited` event and verify with `gh pr checks <PR>` that `pr-title` fails; then `gh pr edit <PR> --title "feat: release-please"` and verify that `pr-title` passes. Keep both run URLs for the PR report
- [ ] 6.4 Run `gh pr comment <PR> --body "@codex review"`, then `python skills/agent-process/scripts/wait_for_pr.py <PR>`, and run both again after each corrective push. Resolve only an addressed older-head P0/P1 thread with `python skills/agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>`. Answer P2/P3 without resolving. If a P0/P1 thread is still open after the third reviewed head, stop pushing and escalate to the person: report the PR, its head, and each unresolved thread's link and one-line finding
- [ ] 6.5 Once `wait_for_pr` settles a green head with no open P0/P1 thread, or at the escalation, report the PR and link the plain-words explanation of the delivered change in the final message. The person merges it

## Scenario → test map

- `distribution` / Version places agree → `tests/publisher/test_plugin.py::test_version_drift`
- `distribution` / Release PR merged → n/a: platform behaviour of release-please-action; observed when the person merges the first release PR (`gh release view v2.1.0` on the merge commit)
- `implementation` / Behaviour change delivered → n/a: procedure prose; observed on this PR's own squash commit `feat: release-please` and on the release PR that proposes `2.1.0` from it
- `implementation` / Title without a type → `tests/publisher/test_reusable_workflows.py::test_pr_title_requires_a_conventional_commit_type` for the check's context, triggers and types; the check's verdict is the action's, observed on this PR by task 6.3 (the failing run on the bare title, the passing run on the typed one); the merge block is the ruleset's, read back with `gh api repos/ekolvah/agent-process-distribution/rules/branches/main` after the person creates it
