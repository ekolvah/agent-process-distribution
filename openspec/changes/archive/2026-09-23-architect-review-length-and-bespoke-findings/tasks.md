## 0. Delivery start

- [x] 0.1 `python skills/agent-process/scripts/start_change.py architect-review-length-and-bespoke-findings --planner Claude --implementer <this carrier>` (Claude or Codex) exits 0 — tracking issue 149; it reads the verdict of the architect review (on `rework` no delivery task runs) and the Status of the issue, creates the linked branch from origin/main, sets In Progress and posts the provenance line; on `propose run not finished` (exit 2) stop — nothing is asked and nothing is created here. (This change's own review is the `.md` of the rule in force when it was planned; task 4.1 changes the read.)

## 1. RED first

- [x] 1.1 `tests/publisher/test_planning_workflow.py` (design D6): `REVIEW_SCHEMA = ROOT / "skills" / "agent-process" / "architect-review.schema.json"`, read once by a `_schema()` helper. `test_review_finding`: `classes.required` is exactly `["simpler", "map", "red", "platform", "replaced", "catcher", "length", "bespoke"]`, every class property has a non-empty `description`, `$defs.class.required == ["evidence", "result"]`, `evidence.minLength == 1`, `result.enum == ["ok", "finding"]`, `then.required == ["finding"]`, `finding.required == ["principle", "artifact", "what", "change"]`; `§I–VII` and `no RED` stay asserted in `_section("Architect review")` and the `red` description respectively. `test_review_sections_carry_their_contents`: `scenario_coverage.items.required == ["capability", "scenario", "reason"]` and its description contains `the reason tasks.md carries`. New `test_over_long_rule_or_bespoke_check`: `length`'s description contains `words its tests assert`, `bespoke`'s contains `observed problem`. `test_asserted_platform_fact`: `platform`'s description contains `asserted, not observed`; `test_untraceable_catcher`: `replaced`'s contains `the Design lists`, `catcher`'s contains `cannot trace`. `test_roles_and_carriers`: `architect-review.json`, `architect-review.schema.json`, `architect-reviewer`, `self-review`, `approve` in `_skill()`; the schema's `required` contains `reviewer` and `reviewer.enum == ["architect-reviewer", "self-review"]`. `test_rework_verdict`: `architect-review.json` in `config["context"]`. `test_verdict_line_the_gate_reads` → the fixture writes a valid JSON review; `verdict()` returns `approve`; `"verdict": "Approved"` raises `ValueError` whose message contains `'Approved' is not one of`. `test_review_archives_with_the_change`: the fixture file is `architect-review.json` with `{"verdict": "approve"}`, the archive glob `*-fixture/architect-review.json`. `test_reviewer_adapter_reads_the_shared_contract`: the reviewer names `skills/agent-process/architect-review.schema.json` and the file exists. Verify `python skills/agent-process/scripts/check_red.py tests/publisher/test_planning_workflow.py::test_review_finding tests/publisher/test_planning_workflow.py::test_review_sections_carry_their_contents tests/publisher/test_planning_workflow.py::test_over_long_rule_or_bespoke_check tests/publisher/test_planning_workflow.py::test_asserted_platform_fact tests/publisher/test_planning_workflow.py::test_untraceable_catcher tests/publisher/test_planning_workflow.py::test_roles_and_carriers tests/publisher/test_planning_workflow.py::test_rework_verdict tests/publisher/test_planning_workflow.py::test_verdict_line_the_gate_reads tests/publisher/test_planning_workflow.py::test_review_archives_with_the_change tests/publisher/test_planning_workflow.py::test_reviewer_adapter_reads_the_shared_contract` exits 0
- [x] 1.2 `tests/publisher/test_delivery_scripts.py`: `_change` writes `architect-review.json` — `json.dumps({"verdict": verdict, "reviewer": "architect-reviewer", "reasoning": "r", "classes": {name: {"evidence": "read", "result": "ok"} for name in the eight}, "scenario_coverage": []})`. `test_verdict_is_rework` adds: `create_tracking_issue.main([_CHANGE, "--priority", "High"])` on the same `rework` fixture with the placeholder token → exit 2, `_creates(gh) == []`. New `test_review_not_valid`: the fixture with `classes.length` deleted, `classes.simpler.evidence` `""` and `classes.map.result` `finding`, on fixtures that create when the file is valid → `start_change.main` on `tracking issue 7` with the default `_Gh()` (Planned) exits 2 with `_develops(gh) == []`, `create_tracking_issue.main([_CHANGE, "--priority", "High"])` on the placeholder token exits 2 with `_creates(gh) == []`, and stderr of each carries `'length' is a required property`, `should be non-empty`, `'finding' is a required property`. New `test_review_validator_absent`: `monkeypatch.setitem(sys.modules, "jsonschema", None)` → `start_change.main` exits 2, stderr carries `architect review not validated`. Verify `python skills/agent-process/scripts/check_red.py tests/publisher/test_delivery_scripts.py::test_verdict_is_rework tests/publisher/test_delivery_scripts.py::test_review_not_valid tests/publisher/test_delivery_scripts.py::test_review_validator_absent` exits 0
- [x] 1.3 `tests/publisher/test_plugin.py::test_package_has_no_installer_state`: the expected file set adds `architect-review.schema.json`. Verify `python skills/agent-process/scripts/check_red.py tests/publisher/test_plugin.py::test_package_has_no_installer_state` exits 0; commit `test(planning): RED for the schema-validated architect review`

## 2. The dependency (D4)

- [x] 2.1 `.agent-process/requirements.in`: add `jsonschema==4.26.0`; regenerate `.agent-process/requirements.txt` with pip-compile as the on-edit hook signals (its transitive pins follow, the header stays). Verify `python -m pip install -r .agent-process/requirements.txt -r .agent-process/requirements-dev.txt` exits 0 and `python -m pip_audit -r .agent-process/requirements.txt` clean; commit `build: pin jsonschema for the architect review`

## 3. The review schema (D1)

- [x] 3.1 Write `skills/agent-process/architect-review.schema.json`, draft 2020-12, as design D1: `verdict` enum `approve`/`rework`; `reviewer` enum `architect-reviewer`/`self-review`, required; `reasoning` non-empty; `classes` with the eight required properties, each `$ref: #/$defs/class` plus a `description` naming the evidence — `simpler`: the simpler design considered and why the scenarios reject it; `map`: each scenario of the spec deltas and its entry of the scenario → test map; `red`: Group 1's tests or its `no RED: <reason>` task; `platform`: each platform behaviour a design rests on and the observation beside it (reference page, run id or command output) — asserted, not observed, is a finding; `replaced`: each replaced input or dropped guard and the Design lists beside it; `catcher`: each named catcher and the delivery step it reaches in the described case — one the review cannot trace is a finding; `length`: for each rule or spec sentence the plan adds or edits, its word count and the count of the words its tests assert; `bespoke`: for each script, check or file the plan adds, the observed problem it closes (issue, PR, run) or `none`; `scenario_coverage` array of `{capability, scenario, reason}`, all required, with the description `every scenario of the spec deltas no test can prove, with the reason tasks.md carries`; `$defs.class` as D1; `additionalProperties: false` everywhere. Verify `python -c "import json, jsonschema; jsonschema.Draft202012Validator.check_schema(json.load(open('skills/agent-process/architect-review.schema.json', encoding='utf-8')))"` exits 0 and `python -m pytest tests/publisher/test_planning_workflow.py tests/publisher/test_plugin.py -q -k "review_finding or sections_carry or over_long or asserted_platform or untraceable or installer_state"` green; commit `feat(planning): the architect review contract is a JSON Schema with eight finding classes`

## 4. The scripts (D2, D3)

- [x] 4.1 `skills/agent-process/scripts/start_change.py`: `SCHEMA = SCRIPT_DIR.parent / "architect-review.schema.json"`; `verdict(change_dir)` as design D3 — `import jsonschema` inside it, `ModuleNotFoundError` → `ValueError(f"architect review not validated: {exc}")`; reads `architect-review.json`; errors `f"{review}: {error.json_path}: {error.message}"` sorted by `error.path`, any → `ValueError` joined by newlines; returns `data["verdict"]`; the module docstring's gate sentence says the review `is valid against architect-review.schema.json beside this skill and its verdict is approve`. Verify `python -m pytest tests/publisher/test_delivery_scripts.py -q -k "not review_not_valid"` and `python -m pytest tests/publisher/test_planning_workflow.py::test_verdict_line_the_gate_reads -q` green
- [x] 4.2 `skills/agent-process/scripts/create_tracking_issue.py`: import `verdict` from `start_change`; `create_tracking_issue()` first calls `verdict(change_dir)` — not `approve` → `print(f"verdict: {line} — apply the findings, re-review (no issue created)", file=sys.stderr)`, return 2; validation errors reach `main()`'s `ValueError` → exit 2; the docstring gains that first step in one sentence. Verify `python -m pytest tests/publisher/test_delivery_scripts.py -q` green; commit `feat(delivery): the propose tail and the apply gate validate architect-review.json`

## 5. The contract and the reviewer (D1, D2, D5)

- [x] 5.1 `skills/agent-process/SKILL.md`, `## Architect review`: from `Write` `architect-review.md` `with three sections` through `is a finding.` becomes: `Write \`architect-review.json\`, valid against \`skills/agent-process/architect-review.schema.json\`.`; after the `create_tracking_issue.py` sentences: `It validates the review first; send its errors back to the reviewer.`; the rest unchanged. `## Tasks`, Group 0: `The script reads the verdict of the architect review` → `The script validates the architect review, reads its verdict`. `openspec/config.yaml` `context`: `openspec/changes/<name>/architect-review.md carries a Verdict that starts with \`approve\`` → `openspec/changes/<name>/architect-review.json is valid with verdict \`approve\``. `agents/architect-reviewer.md`: `architect-review.md` → `architect-review.json` on lines 3, 11 and 27; step 1 adds `and \`skills/agent-process/architect-review.schema.json\`` and drops `(the review is not a schema artifact, so no \`openspec\` command describes it)`. Verify `python -m pytest tests/publisher -q` green and `python -c "import yaml; yaml.safe_load(open('openspec/config.yaml', encoding='utf-8'))"` exits 0; commit `feat(planning): the skill and the reviewer name the JSON review and its schema`

## 6. The record (D4, D5)

- [x] 6.1 `architect-review.md` → `architect-review.json` in `AGENTS.md` line 27, `.claude/rules/workflow.md` line 19, `.agents/orchestration/roles.yaml` lines 21–22 (`is stated in the Verdict line of \`architect-review.md\` (the \`tasks\` rule of \`openspec/config.yaml\`)` → `is stated in the \`reviewer\` field of \`architect-review.json\` (the skill's review schema)`), 66, 87 (`whose Verdict line starts with \`approve\`` → `valid against the review schema with verdict \`approve\``) and 104; `.agent-process/docs/architecture/agent-process.md`, Planning: the file name and `(a scenario missing from the scenario → test map is a finding, and so is an untraceable catcher)` → `(one entry per finding class of the skill's \`architect-review.schema.json\`; \`create_tracking_issue.py\` refuses a file the schema rejects)`; ADR 0027, one bullet after the issue 146 entry: the review is a JSON file validated against a JSON Schema in the skill — the prose finding list was read past (round 1 of this change wrote `none` with `simpler` in the contract); the eight classes are the schema's; a plugin agent's frontmatter `hooks` are ignored (sub-agents docs), so the propose tail and the apply gate validate, with `jsonschema` imported on the call; a consumer's `jsonschema` is a prerequisite #155 names; `codex --output-schema` rejected (`'if' is not permitted`). Verify `python -m pytest tests/agent_process/test_doc_headers.py tests/agent_process/test_doc_links.py tests/agent_process/test_doc_narrative.py tests/agent_process/test_agent_orchestrator.py -q` green; commit `docs(planning): architect-review.json and the schema in the process record`

## 7. Verify

- [x] 7.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid and `python .agent-process/scripts/ci_check.py` green; `git status --short` shows nothing of this change uncommitted

## 8. Deliver

- [x] 8.1 `git status --short` empty for this change's files → `python skills/agent-process/scripts/archive_change.py architect-review-length-and-bespoke-findings` (marks its own task, archives, commits, pushes)
- [ ] 8.2 `gh pr create --title "architect-review-length-and-bespoke-findings" --body-file <report>` — the change name, issue 149 as a plain reference (no `Closes`), Why / What, the scenario → test map below, no deferral beyond #155's prerequisite; ends with the Claude Code footer
- [ ] 8.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` after the PR and after every push → `python skills/agent-process/scripts/wait_for_pr.py <PR>` → apply every unresolved thread, push, re-request, `wait_for_pr.py <PR>` again; a `P0`/`P1` thread the push addressed: `python skills/agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>` (`--list` prints the open ones); a `P2`/`P3` thread is answered, never resolved by the process; a fix that changes a spec goes through a change of its own on the branch (`npx -y @fission-ai/openspec@1.13.0 new change <name>`, the delta, `validate --strict`, `archive_change.py <name>`), never a direct edit of `openspec/specs/`; a fix that changes a design decision amends the archived `design.md` beside it and the map of the archived `tasks.md` in the same push; a finding on what a script does is closed by its class; at most three rounds, the fourth leaves the rest to the person with a reply. The person merges. No tick after the archive; a run interrupted after it continues from `gh pr view architect-review-length-and-bespoke-findings`

## Scenario → test map

- planning / Review finding → `tests/publisher/test_planning_workflow.py::test_review_finding` (task 1.1: the schema requires the eight classes, each with evidence and a result, a finding with its four fields)
- planning / Review class without evidence → `tests/publisher/test_delivery_scripts.py::test_review_not_valid` (task 1.2: `create_tracking_issue` and `start_change` refuse the file with the three messages, nothing created) and `::test_review_validator_absent` (task 1.2: no validator is exit 2, not a pass); the planner sending the errors back to the reviewer → n/a: a step of the propose run the skill names, in a session pytest does not drive
- planning / Over-long rule or bespoke check → `tests/publisher/test_planning_workflow.py::test_over_long_rule_or_bespoke_check` (task 1.1); the reviewer's count on a given plan → n/a: the evidence line the person reads at solution review, not a script
- planning / Rework verdict → `tests/publisher/test_planning_workflow.py::test_rework_verdict` and `tests/publisher/test_delivery_scripts.py::test_verdict_is_rework` (task 1.2: both scripts refuse `rework`)
- planning / Plan approved → `tests/publisher/test_planning_workflow.py::test_plan_approved`, `tests/publisher/test_delivery_scripts.py::test_plan_approved_creates_the_issue` (the fixture is the valid JSON of task 1.2)
- planning / Existing tracking issue → `tests/publisher/test_delivery_scripts.py::test_existing_tracking_issue` (unchanged; the JSON fixture)
- planning / Review archives with the change → `tests/publisher/test_planning_workflow.py::test_review_archives_with_the_change` (task 1.1)
- implementation / Verdict is rework → `tests/publisher/test_delivery_scripts.py::test_verdict_is_rework` and `::test_review_not_valid` (task 1.2)
- implementation / Tasks of a new change, Propose run stopped before its tail, Blocking thread addressed, Review fix changes a spec, Design decision changed at review, Finding closed by its class → unchanged scenarios of the modified requirement, already proved by `tests/publisher/test_planning_workflow.py::test_tasks_of_a_new_change`, `::test_plan_approved`, `::test_design_decision_changed_at_review`, `::test_finding_closed_by_its_class` and `tests/publisher/test_delivery_scripts.py::test_tasks_of_a_new_change_start`, `::test_propose_run_stopped_before_its_tail`; task 5.1 keeps them green
- roles / Codex plans a change → `tests/publisher/test_planning_workflow.py::test_roles_and_carriers` (task 1.1: the skill names `architect-review.json`, the schema and the self-review; the schema requires `reviewer` with `self-review` among its values); the Codex run → n/a: the same `create_tracking_issue` validation `test_review_not_valid` runs, in a session pytest does not drive
