## 0. Delivery start

- [x] 0.1 Gate: `grep -q "^approve" openspec/changes/v2-2c-pr-link-native/architect-review.md` exits 0 (on `rework`: apply the findings, re-review, no delivery task runs) and `gh issue view 131 --json projectItems --jq '.projectItems[0].status.name'` prints `Planned`; otherwise print `propose run not finished: run its tail (the Architect review entry of this rule) first` and stop — nothing is asked and nothing is created here
- [x] 0.2 Branch: `git fetch origin && git checkout main && git pull` → `gh issue develop -c 131 --name v2-2c-pr-link-native`; verify `git branch --show-current` prints the change name
- [x] 0.3 `python .agent-process/scripts/set_status.py 131 "In Progress"` exits 0 (the priority is already set on the issue)
- [x] 0.4 Provenance: `gh issue comment 131 --body "planner: Claude; implementer: <this carrier>"` (Claude or Codex)

## 1. RED first

- [x] 1.1 `tests/publisher/test_reusable_workflows.py`: add `test_quality_verifies_the_pr_links_its_issue_before_the_driver` — `_steps("reusable-quality.yml")` has `Verify the PR links its issue` before `Checkout trusted quality driver` (no checkout needed: `gh` reads the API); its `env` is `{"GH_TOKEN": "${{ github.token }}", "GH_REPO": "${{ github.repository }}", "PR": "${{ github.event.pull_request.number }}"}`; its `run` contains `--json closingIssuesReferences`, `.closingIssuesReferences | length`, `::error::`, `Closes #N`, `gh issue develop -c`, `gh run rerun $GITHUB_RUN_ID` and `exit 1`, `run.count("gh pr view") == 1` and no `sleep` (one read, no poll), no `verify_pr_link`, and `"shell" not in step` (D1: `bash -e {0}` is what makes a failed read red); `_workflow("reusable-quality.yml")["permissions"]` and `_workflow("ci.yml")["permissions"]` both equal `{"contents": "read", "pull-requests": "read", "issues": "read"}`; `_trigger(_workflow("ci.yml"))["pull_request"]` is `None` (no `edited`: the re-run is the fixer's). Add `test_the_pr_link_gate_is_gone` — `WORKFLOWS / "pr-link.yml"`, `WORKFLOWS / "reusable-pr-link.yml"` and `ROOT / ".agent-process" / "scripts" / "verify_pr_link.py"` do not exist. In the same file drop `reusable-pr-link.yml` from `test_callees_declare_workflow_call_without_pull_request_trigger`, the `pr-link` tuple from `test_callee_schema_matches_local_callers_in_both_directions` and from `test_caller_permissions_are_a_superset_of_callee_permissions`, and delete `test_pr_link_uses_a_bootstrap_fallback_only_when_main_has_no_driver`, `test_pr_link_grants_issues_read`, `test_pr_link_feature_detects_deferred_scope_support`, `test_pr_link_installs_its_driver_dependencies` (the workflow they describe is deleted). `tests/publisher/test_deferred_scope.py`: delete both `verify_pr_link` sections (from the `# verify_pr_link: soundness, not equality` banner to the end of the file, `_IssueReads` and `_stub_gh_seams` with them), drop `verify_pr_link` from the `from scripts import …` line, `json` from the imports when nothing else uses it, and `verify_pr_link.py` from the module docstring's list. `tests/agent_process/test_branch_protection.py::TestDriftDetection::test_controller_gate_is_not_a_required_context`: the tuple is `("quality / quality", "agent-review / agent-review")`; the docstring of the module keeps its `pr-link` history. Run `python -m pytest --junitxml=.pytest-report.xml tests/publisher/test_reusable_workflows.py tests/publisher/test_deferred_scope.py tests/agent_process/test_branch_protection.py -q`, verify `python .agent-process/scripts/check_red.py --report .pytest-report.xml tests/publisher/test_reusable_workflows.py::test_quality_verifies_the_pr_links_its_issue_before_the_driver tests/publisher/test_reusable_workflows.py::test_the_pr_link_gate_is_gone tests/agent_process/test_branch_protection.py::TestDriftDetection::test_controller_gate_is_not_a_required_context` exits 0; commit `test(review-and-merge): RED for the native PR link step and two required contexts`

## 2. The link step of the quality callee (D1, D3, D4)

- [x] 2.1 `.github/workflows/reusable-quality.yml`: `permissions` gains `pull-requests: read` and `issues: read`; first step of the job, before `Checkout trusted quality driver`: `name: Verify the PR links its issue`, `env` `GH_TOKEN: ${{ github.token }}`, `GH_REPO: ${{ github.repository }}`, `PR: ${{ github.event.pull_request.number }}`, no `shell:` key, `run`: `linked="$(gh pr view "$PR" --json closingIssuesReferences --jq '.closingIssuesReferences | length')"`; when `"$linked" = "0"`: `echo "::error::PR $PR links no issue. Link one — \`Closes #N\` in the PR body, or open the PR from the branch of \`gh issue develop -c N --name <branch>\` — then \`gh run rerun $GITHUB_RUN_ID\`."` and `exit 1`; else `echo "ok: PR $PR links $linked issue(s)"`. The comment above the step names the observation (the link is computed within seconds of the PR, read once, no poll; `GH_REPO` because no checkout precedes the step; a failed read is red under `bash -e`; ADR 0027, `v2-2c`). `.github/workflows/ci.yml`: `permissions` gains the same two scopes. Verify `python -m pytest tests/publisher/test_reusable_workflows.py -q -k "quality_verifies_the_pr_links or caller_permissions"` green (`test_the_pr_link_gate_is_gone` stays red until 3.1); commit `feat(review-and-merge): the quality callee reads closingIssuesReferences before the driver`

## 3. The v1 gate goes (D2, D5 — repository side)

- [x] 3.1 Delete `.github/workflows/pr-link.yml`, `.github/workflows/reusable-pr-link.yml`, `.agent-process/scripts/verify_pr_link.py` (`git rm`). `.agent-process/scripts/check_branch_protection.py`: `REQUIRED_CONTEXTS = ("quality / quality", "agent-review / agent-review")`; the comment beside it, when it names three, names two. `.agent-process/scripts/open_pr.py` line 117: the comment `Code 2, as in sibling verify_pr_link.py` → `Code 2: infrastructure failure must differ` (the rest of the sentence stays). Verify `python -m pytest tests/publisher/test_reusable_workflows.py tests/publisher/test_deferred_scope.py tests/agent_process/test_branch_protection.py tests/agent_process/test_delivery_gate_wiring.py -q` green (`test_the_pr_link_gate_is_gone` and `test_controller_gate_is_not_a_required_context` turn green here) and `python .agent-process/scripts/check_branch_protection.py` exits 0 printing `pr-link / pr-link` as preserved (GitHub still requires three: an extra context is not drift); commit `refactor(review-and-merge): delete the pr-link workflows and verify_pr_link; two required contexts`

## 4. The record (D2, D5, D6, D7)

- [x] 4.1 `.agent-process/docs/architecture/agent-process.md`, Issue contract (lines 54–72): the `## Deferred scope` paragraph says the export is v1 and inert — nothing verifies it since `v2-2c`, the block goes with `open_pr.py` (issue 114) — and the sentence on `REVIEW_CONTRACT.md` and the `pr-link` driver names `REVIEW_CONTRACT.md` alone; one sentence: every PR links its issue (`closingIssuesReferences`: the branch of `gh issue develop`, or `Closes #N`), read by a step of `quality`, a missing link fails the check and the fixer re-runs it after linking. `.agent-process/docs/architecture/agent-process-installation.md` (lines 262–269): two callers (`ci.yml`, `agent-review.yml`), two contexts, `ci.yml` with `pull-requests: read` and `issues: read`. `.agent-process/docs/adr/0027-v2-standards-replace-the-bespoke-control-plane.md`: one bullet after the issue-138 entry — `The PR → issue link is GitHub's field (issue 131, v2-2c-pr-link-native)`: the four observations of proposal.md in one sentence each (the `closingIssuesReferences` of PR 141/140/136/135/133 without a keyword; PR 141 created `15:31:31Z`, connected `15:31:34Z`, run `35362952165` step at `15:31:45Z`, PR 140 the same shape; run `35364647176` printing `ok` on a branch the gate never read; classic protection, `rulesets` empty), what is deleted and why the check moved into `quality` (D1, D2), the exemption rule for a bot PR (none until observed), the bootstrap (D5: the context removed by the reference `DELETE` call after the checks of the PR are green, by the person or on their confirmation) and the D6 line `<confirmed on the first PR after the merge: run id, the step's output>`; deletion condition: the step goes when GitHub requires a linked issue natively. The superseded list of More Information gains `ADR 0020 (a tracked deferral downgrades a matching review finding) → nothing: the downgrade left the contract in v2-2b, the verification leaves with verify_pr_link (v2-2c)`. `.agent-process/copier-answers.yml` lines 34 and 38: the two lines of the dead record drop `pr-link` (the file is the v1 render answer set, deleted by issue 112). Verify `python -m pytest tests/agent_process/test_doc_headers.py tests/agent_process/test_doc_links.py tests/agent_process/test_doc_narrative.py tests/publisher/test_reusable_workflows.py -q` green (`test_installation_documents_the_caller_workflow_trust_boundary` keeps `issues: read`); commit `docs(review-and-merge): ADR 0027 entry, Issue contract and installation for the native PR link`

## 5. Verify

- [x] 5.1 `npx -y @fission-ai/openspec@1.13.0 validate --strict --all` valid and `python .agent-process/scripts/ci_check.py` green; `git status --short` shows nothing of this change uncommitted

## 6. Deliver

- [ ] 6.1 `git status --short` empty for this change's files → `python .agent-process/scripts/archive_change.py v2-2c-pr-link-native` (marks its own task, archives, commits, pushes; the pre-push drift check passes: `pr-link / pr-link` on GitHub is preserved, not missing)
- [ ] 6.2 `gh pr create --title "v2-2c-pr-link-native" --body-file <report>` — the change name, issue 131 as a plain reference (no `Closes`), Why / What, the scenario → test map below, the bootstrap note (the PR shows `pr-link / pr-link` expected and never reported until step 6.4), the deferral: the first exercise of the step is the PR after the merge (D6); ends with the Claude Code footer
- [ ] 6.3 `python .agent-process/scripts/request_codex_review.py --request <PR>` after the PR and after every push → `python .agent-process/scripts/wait_for_pr.py <PR>` → apply every unresolved thread, push, re-request, `wait_for_pr.py <PR>` again; a `P0`/`P1` thread the push addressed: `python .agent-process/scripts/resolve_review_thread.py --repo ekolvah/agent-process-distribution --pr <PR> --thread <id> --reply-file <path>` (one thread per rerun cycle, `wait_for_pr.py` between); a `P2`/`P3` thread is answered; a fix that changes a spec goes through a change of its own on the branch; at most three rounds, the fourth leaves the rest to the person with a reply. No tick after the archive; a run interrupted after it continues from `gh pr view v2-2c-pr-link-native`
- [ ] 6.4 Bootstrap (D5), after 6.3 ends with `quality` and `agent-review` green and no open `P0`/`P1` thread: ask the person to remove the context — or, on their confirmation, `gh api --method DELETE repos/ekolvah/agent-process-distribution/branches/main/protection/required_status_checks/contexts --input <file>` with `{"contexts": ["pr-link / pr-link"]}` — then `python .agent-process/scripts/check_branch_protection.py` prints `quality / quality` and `agent-review / agent-review` and nothing preserved, and `gh pr view <PR> --json mergeStateStatus` prints `CLEAN`. The person merges. The D6 line of ADR 0027 is filled by the implementer of the next change from its PR's first `quality` run

## Scenario → test map

- review-and-merge / PR from a linked branch → `tests/publisher/test_reusable_workflows.py::test_quality_verifies_the_pr_links_its_issue_before_the_driver` (task 1.1: the step, its read, its position before the driver, the permissions); the read of a live PR → n/a: the callee is pinned `@main`, the PR of this change runs today's callee; the first PR after the merge is the observation (D6, ADR 0027 line)
- review-and-merge / PR without an issue → the same test (task 1.1: the `::error::` text names `Closes #N`, `gh issue develop -c`, `gh run rerun $GITHUB_RUN_ID`, and `exit 1` before the driver step); a red run on a live PR without an issue → n/a: no PR of this repository is opened without a tracking issue (Group 0 of every change); the message is asserted, the exit is the shell's
- review-and-merge / Missing required context → `tests/agent_process/test_branch_protection.py::TestDriftDetection::test_controller_gate_is_not_a_required_context` (task 1.1: the two contexts declared) and `tests/publisher/test_reusable_workflows.py::test_the_pr_link_gate_is_gone` (task 1.1: the caller of the third context no longer exists); the live removal of `pr-link / pr-link` → n/a: task 6.4, the person's action on the platform, verified by `check_branch_protection.py` and `mergeStateStatus`
