name: Agent review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  agent-review:
    uses: {{ workflow_references["agent-review"] | tojson }}
    with:
      codex-timeout-seconds: 600
