name: PR link

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: read
  issues: read

jobs:
  pr-link:
    uses: {{ workflow_references["pr-link"] | tojson }}
