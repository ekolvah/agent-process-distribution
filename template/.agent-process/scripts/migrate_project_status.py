#!/usr/bin/env python3
"""Migrate a legacy Agent status field into built-in Status."""


def build_plan(fields: object, items: object, views: object) -> object:
    """Return the migration plan; implemented after RED evidence."""
    raise NotImplementedError
