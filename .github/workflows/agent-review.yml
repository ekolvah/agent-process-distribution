name: Agent review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  agent-review:
    uses: "./.github/workflows/reusable-agent-review.yml"
    with:
      codex-timeout-seconds: 600
