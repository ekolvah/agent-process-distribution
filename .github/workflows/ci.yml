name: CI

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read
  issues: read

jobs:
  quality:
    uses: "ekolvah/agent-process-distribution/.github/workflows/reusable-quality.yml@main"
